import {
    ListChecksIcon,
} from "lucide-react";
import { EditorFormSchma } from "./core";

export const CheckboxSchema: EditorFormSchma = {
    name: '多选题',
    icon: ListChecksIcon,
    key: 'checkbox',
}