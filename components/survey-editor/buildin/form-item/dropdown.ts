import {
    BarChart4,
} from "lucide-react";
import { EditorFormSchma } from "./core";

export const DropdownSchema: EditorFormSchma = {
    name: '下拉选择',
    icon: BarChart4,
    key: 'dropdown',
}